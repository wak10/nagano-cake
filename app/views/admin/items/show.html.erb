<h2>商品詳細画面</h2>
<div>
  <%= attachment_image_tag @items, :image, :fill, 300, 300 %>
  <p>商品名：<%= @items.name %></p>
  <p>商品説明：<%= @items.introduction %></p>
  <p>ジャンル：<%= @items.genre.name %></p>
  <p>税込価格</br>(税抜価格)：<%= @items.price %></p>
  <p>
    販売ステータス：<% if @items.is_active == true %>
        販売中
      <% else %>
        販売停止中
      <% end %>
  </p>

  <%= link_to "編集する", edit_admin_item_path(@items) %>

</div>