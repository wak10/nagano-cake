  <div class="d-inline-flex m-4">
    <h4 class="bg-light px-4 align-items-center">商品詳細</h4>
  </div>
  <div class="d-flex align-items-center justify-content-center">
    <div style="margin-right:100px">
      <%= attachment_image_tag @items, :image, :fill, 300, 300 %>
    </div>
    <div style="margin-right:100px">
      <p>商品名</p>
      <p>商品説明</p>
      <p>ジャンル</p>
      <p>税込価格</br>(税抜価格)</p>
      <p>販売ステータス</p>
    </div>
    <div>
      <p><%= @items.name %></p>
      <p><%= @items.introduction %></p>
      <p><%= @items.genre.name %></p>
      <p class="my-4"><%= @items.with_tax_price %>(<%= @items.price %>)円</p>
      <p class="p-2">
        <% if @items.is_active == true %>
          販売中
        <% else %>
          販売停止中
        <% end %>
        </p>
    </div>
  </div>
  <div class="container">
    <div class="d-flex align-items-center justify-content-center">
      <%= link_to "編集する", edit_admin_item_path(@items), class: "btn btn-success" %>
    </div>
  </div>