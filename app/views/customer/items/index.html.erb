<div>
<h2>商品一覧</h2>
<span>(全</span>
<% @all_items.count %>
<%= @all_items.where(is_active: "true").count %>
<span>件)</span>
</div>


<% @items.each do |item| %>
  <div>
    <%= link_to item_path(item) do %>
      <%= attachment_image_tag item, :image, :fill, 200, 200 %>
      <%= item.name %>
    <% end %>
    <spam>¥</spam>
    <%= item.price %>
  </div>
<% end %>
  <%= paginate(@items) %>